handler: src/uploader/index.handle
role: UploadRoleBucket
events:
  - http:
      path: get-presigned-url
      method: get
      cors:
        origin: '*'
        headers: '*'
      authorizer: ${self:custom.tokenAuthorizer}
timeout: 20

package:
  patterns:
    - src/uploader/index.js
